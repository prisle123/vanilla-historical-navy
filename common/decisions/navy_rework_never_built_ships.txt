navy_rework_category = {
    add_never_built_blueprints = {
        icon = generic_naval
        fire_only_once = yes
        allowed = {
            OR = {
                tag = ENG
                tag = FRA
                tag = GER
                tag = ITA
                tag = JAP
                tag = SOV
                tag = USA
            }
        }
        complete_effect = {
            FROM = {
                if = {
                    limit = { original_tag = ENG }
                    ENG_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = FRA }
                    FRA_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = GER }
                    GER_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = ITA }
                    ITA_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = JAP }
                    JAP_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = SOV }
                    SOV_never_built_blueprints = yes
                }
                if = {
                    limit = { original_tag = USA }
                    USA_never_built_blueprints = yes
                }
            }
        }
    }

    add_wunderwaffe_ships = {
        icon = generic_naval
        visible = {
            OR = {
                NOT = { has_country_flag = spawn_alicorn }
                NOT = { has_country_flag = spawn_constitution }
                NOT = { has_country_flag = spawn_macross }
                NOT = { has_country_flag = spawn_space_yamato }
            }
        }
        complete_effect = {
            hidden_effect = { country_event = navy_rework.1 }
        }
    }
}